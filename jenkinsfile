pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean'
            }
        } 
        stage('Build') {
            steps {
                sh 'mvn clean'
            }
        }
        
    }
}
